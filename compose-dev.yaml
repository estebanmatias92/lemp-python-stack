version: "3.8"
name: vanilla-python
services:
  app:
    build:
      context: .
      target: development
      args:
        - WORKDIR=${WORKDIR}
        - USER=${USER}
        - VENVPATH=${VENVPATH}
    env_file: .env