name: vanilla-python
services:
  app:
    build:
      context: .
      target: development
    env_file: .env.dev
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock